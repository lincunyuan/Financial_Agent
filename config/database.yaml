# 数据库连接配置

# Redis配置（用于会话管理）
redis:
  host: "localhost"
  port: 6379
  db: 0
  password: "123"  # 如果需要密码，请填入

# MySQL配置（用于存储文档元数据）
mysql:
  host: "localhost"
  port: 3306
  user: "root"
  password: "lincy123"  # 请填入MySQL密码
  database: "financial_rag"  # 数据库名称，如果不存在会自动创建

# Milvus配置（用于向量存储）
milvus:
  host: "localhost"
  port: 19530
  # user: ""  # 如果需要认证，请填入
  # password: ""