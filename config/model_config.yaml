# 大模型配置

# LLM提供商: "openai"（支持OpenAI API和兼容模型如qwen-plus）或 "local"
provider: "openai"

# OpenAI/兼容模型配置
api_key: "sk-9494f10802d94c40b1a0ef5c54c67a60"  # 用户提供的新API Key
model: "qwen-plus-0723"  # 用户提供的模型名称
base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"  # 设置为阿里云DashScope的兼容模式地址

# 本地模型配置（如Ollama）
# provider: "local"
# base_url: "http://localhost:11434"  # Ollama默认地址
# model: "llama2"  # 或其他本地模型名称

# 生成参数
max_tokens: 1000
temperature: 0.7